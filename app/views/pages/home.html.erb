    <section class="home">
    </section>


<h1>ROOT LANGDINGPAGE WITH CTA</h1>
<p>This is where any user/ visitor lands first</p>


<%= link_to 'Dashboard', funerals_dashboard_path %>


 <!-- Add this if markers are possible data-markers="<%= @markers.to_json %>" -->
<div id="map"
     style="width: 100%; height: 600px;"
     data-markers="<%= @markers.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>
