<h1>My playlist</h1>
<p>My URL</p>

<%= @playlist.spotify_url %>
<br>

  <%= simple_form_for(@playlist) do |f| %>
    <%= f.error_notification %>
    <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
    <div class="form-inputs p-5 d-flex justify-content-center flex-column">
      <%= f.input :spotify_url, wrapper_html: {class: "form-group d-flex align-items-center flex-column"}%>
    <div class="form-actions">
      <%= f.button :submit, class: "btn btn-obnb-main-lg" %>
    </div>
  <% end %>

