<%#= link_to 'Back to overview', funerals_dashboard_path, class: 'btn btn-light m-1'%>
<div class="container d-flex align-items-center justify-content-center flex-row">
  <div>
    <h2>"My music</h2>
    <p>There are many ways to express meaningful last words to your loved ones. Music can help you finding the right last words. Create your personal life's sioundtrack in Spotify and connect your playlist in here. The playlist will become part of your obituary page and will invoke all sorts of shared memories for your loved ones.</p>
  </div>
  <%= image_tag "i_music.png", class: "illustration-music" %>
</div>

  <div class="container d-flex justify-content-center align-items-start">
    <div class="container d-flex justify-content-center align-items-center flex-column">
      <div class="form-design">
        <%= simple_form_for(@playlist) do |f| %>
          <%= f.error_notification %>
          <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
          <h4 class="d-flex flex-center">Playlist</h4>
          <h2 class="d-flex flex-start">The soundtrack of your life</h2>
            <div class="form-inputs p-5 d-flex justify-content-center flex-column">
              <%= f.input :spotify_url, input_html: {class: 'textfield-music'}, label: 'Insert your spotify url below and save it for your descendants 😇 ' %>
              <%= f.input :spotify_profile_url, input_html: {class: 'textfield-music'}, label: 'Connect your entire spotify profile URI here for your descendantsfollow all of your playlists' %>
                <div class="form-actions d-flex justify-content-center align-items-center flex-column mt-5">
                  <%#= f.button :submit, class: "btn btn-lj-big-g" %>
                  <%= f.button :submit, value: "Save", class: "btn btn-lj-big-m2 mb-2" %>
                  <%= f.submit name: "redirect_to", value: "Save & exit", class: "btn btn-lj-big-m4", label: "Save & Leave" %>
                </div>
            <% end %>
            </div>
      </div>
    </div>

    <div class="container d-flex justify-content-center align-items-center flex-column">
      <% if @spotify_profile_url.present? %>
        <div class = "d-flex justify-content-center align-items-center mt-5">
          <iframe src="https://open.spotify.com/follow/1/?uri=<%=@spotify_profile_url%>&size=detail&theme=light" width="220" height="56" scrolling="no" frameborder="0" style="border:none; overflow:hidden;" allowtransparency="true"></iframe>
        </div>
      <% end %>

      <% if @embed_spotify_url.present? %>
        <div class = "d-flex justify-content-center align-items-center spotifyplayer mt-5">
          <iframe src="https://open.spotify.com/embed/playlist/<%=@embed_spotify_url %>" width="100%" height="400" frameborder="0" allowtransparency="true" allow="encrypted-media"></iframe>
        </div>
      <% end %>
  </div>
</div>

      <!-- <div class="d-flex justify-content-center">
          <%#= link_to 'Back to overview', funerals_dashboard_path, class: 'btn btn-secondary btn-lg btn-block'%>
      </div> -->
